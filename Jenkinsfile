@Library('sharedlib-demo')_

pipeline {
    agent any

    stages {
        stage('Demo') {
            steps {
                welcome("Shubham Kumar")
                calculator(10,50)
            }
        }
    }
}
